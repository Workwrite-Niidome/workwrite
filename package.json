{
  "name": "workwrite",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:check": "bash scripts/check-services.sh && turbo dev",
    "services:up": "docker compose up -d",
    "services:down": "docker compose down",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,css,md}\""
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "prettier": "^3.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@11.8.0"
}
